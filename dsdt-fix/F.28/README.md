# dsdt-fix/F.28 — ⚠️ FAKE（测试用）

> **本目录不是真实的 F.28 补丁。**

用途：验证 install.sh 的「按 BIOS 版本路由 + 版本选择」逻辑。

- `dsdt.aml`：**内容与 `../F.29/dsdt.aml` 完全相同**（复制占位），并非从 F.28 固件 dump 制作。
- 真实场景中，F.28 需要从 F.28 固件 dump → 反汇编 → 逐处适配 → 编译，不能直接复制。

请在 README 或 issue 里注明这是测试数据，避免被当作真实补丁使用。
