# DSDT override — <BOARD>/<BIOS>

> Template — copy me into `dsdt-fix/<board>/<bios>/README.md`, fill in the
> fields, delete this note. `dsdt-fix.sh` prints this file before installing
> the patch.

Built on **<product>, BOARD <BOARD> BIOS <BIOS>** tested on **<distro>**
by **<you>**.

Changes in this patch:
1. <change and why>
2. <...>

Per-change rationale and sources are annotated in `patch.diff` if any.
